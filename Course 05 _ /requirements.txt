# AIAT 115 - Scalable Data Science
# Python Package Requirements

# Core Data Processing (CPU)
pandas>=2.0.0
numpy>=1.24.0

# Machine Learning (CPU)
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0

# Distributed Computing
dask>=2023.8.0
dask[dataframe]>=2023.8.0

# Utilities
jupyter>=1.0.0
ipython>=8.12.0
notebook>=7.0.0

# Data Handling
openpyxl>=3.1.0  # For Excel files
scipy>=1.10.0    # Scientific computing

# Data Quality
missingno>=0.5.2  # Missing data visualization

# Optional but recommended (commented out - doesn't support Python 3.13+)
# pandas-profiling>=3.6.0  # For data exploration - use ydata-profiling instead
ydata-profiling>=4.0.0  # Modern alternative to pandas-profiling

# Note: NVIDIA RAPIDS packages (cudf, cuml, cuPy) should be installed separately
# via conda or pip according to your CUDA version. See installation guide:
# https://rapids.ai/start.html
#
# Example conda installation:
# conda create -n rapids-env -c rapidsai -c conda-forge -c nvidia cudf=23.08 cuml=23.08 python=3.10 cudatoolkit=11.8
#
# Or via pip (limited GPU support):
# pip install cudf-cu11 cuml-cu11 --extra-index-url=https://pypi.nvidia.com

